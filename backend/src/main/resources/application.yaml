spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mailnest
    username: MAIL_USERNAME
    password: MAIL_PASSWORD
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: bthaya11@gmail.com
    password: tfounpvpzqindemw  # app password generated by google with no spaces
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080
  error:
    include-message: always

